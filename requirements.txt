# Core runtime dependencies for the prototype and the URL shortener demo.
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# Test / validation toolchain (used by the Validator quality gate).
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
